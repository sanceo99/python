#This program asks the user for a string and print out whether this string is a palindrome or not

while True :
        text=input()
        invText=text[::-1]

        if text==invText:
                print('Your word is a palindrome')
        else:
                print('Your word is not a palindrome')
